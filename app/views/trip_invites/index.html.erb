<h1>Listing trip_invites</h1>

<table>
  <tr>
    <th>Trip</th>
    <th>Sender</th>
    <th>Receiver</th>
    <th>Accepted</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @trip_invites.each do |trip_invite| %>
  <tr>
    <td><%= trip_invite.trip %></td>
    <td><%= trip_invite.sender %></td>
    <td><%= trip_invite.receiver %></td>
    <td><%= trip_invite.accepted %></td>
    <td><%= link_to 'Show', trip_invite %></td>
    <td><%= link_to 'Edit', edit_trip_invite_path(trip_invite) %></td>
    <td><%= link_to 'Destroy', trip_invite, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Trip invite', new_trip_invite_path %>
