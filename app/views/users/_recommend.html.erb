<h5>You may know</h5>
<ul id="recommendations">
<% @friendsOfFriends.each do |friendOfFriend| %>
<li>
		<%= link_to User.find(friendOfFriend).name, user_path(friendOfFriend) %></li>
		<% end %>
		<ul>