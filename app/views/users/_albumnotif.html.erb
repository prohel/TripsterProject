<div class="well well-sm">
<%= glyph(:plus) %>
<%album = Album.find(n[:target_id])%>
<%user = User.find(n[:owner_id])%>
<%trip = Trip.find(album.trip_id)%>
Album <strong><%= album.name%></strong> was created by <strong><%=user.name%></strong> in trip <strong> <%= trip.name%></strong><strong><span style="float:right"><%= local_time(n[:created_at])%></span></strong>
<div style="margin-top:10px">
<div class="likes"><%= pluralize(album.likers(User).count, 'like')%></div>
</div>
<%= render 'shared/like_form', likeable: album %>
</div>